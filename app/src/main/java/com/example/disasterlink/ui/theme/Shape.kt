package com.example.disasterlink.ui.theme

import androidx.compose.foundation.shape.RoundedCornerShape
import androidx.compose.material3.Shapes
import androidx.compose.ui.unit.dp

val Shapes = Shapes(
    small = RoundedCornerShape(8.dp),    // for controls or chips
    medium = RoundedCornerShape(16.dp),  // for cards, buttons
    large = RoundedCornerShape(24.dp)    // for modals, large containers
)
